{
  "name": "parallels-board",
  "private": "true",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start:backend": "npm run start --workspace=backend",
    "start:frontend": "npm run start --workspace=frontend"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  }
}
